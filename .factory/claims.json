[
  {
    "id": "demo-one-click",
    "milestone": "M1",
    "claim": "Try a ready client action room in one click",
    "where": "landing primary action, README",
    "test": "npm run test:e2e -- --grep @claim:demo-one-click",
    "sandbox": "fresh browser context; open /; activate the one primary action; assert /demo has the seeded Northline Studio queue"
  },
  {
    "id": "demo-reset",
    "milestone": "M1",
    "claim": "Demo changes are sample-only and resettable",
    "where": "demo banner, landing privacy section, README",
    "test": "npm run test:e2e -- --grep @claim:demo-reset",
    "sandbox": "two fresh browser contexts; change and complete the sample approval in one; assert the other remains unchanged; reset the first and assert the exact seed returns"
  },
  {
    "id": "client-no-account",
    "milestone": "M1",
    "claim": "A client can approve a request without creating an account",
    "where": "landing fact, demo client window, README",
    "test": "npm run test:e2e -- --grep @claim:client-no-account",
    "sandbox": "fresh demo; publish the sample approval; open its client fragment link in a signed-out context; approve; assert completion without Entra navigation or account input"
  },
  {
    "id": "deadline-order",
    "milestone": "M1",
    "claim": "Open requests are ordered by deadline",
    "where": "landing live preview, demo deadline rail",
    "test": "npm run test:e2e -- --grep @claim:deadline-order",
    "sandbox": "fresh seeded demo with fixed clock; read all open action due times and assert ascending order with the overdue request first"
  },
  {
    "id": "approval-audit",
    "milestone": "M1",
    "claim": "An approval records the decision, actor label, and server time",
    "where": "demo completion result, audit ledger",
    "test": "npm run test:e2e -- --grep @claim:approval-audit",
    "sandbox": "fresh demo with fixed server clock; submit approval as Maya Chen; return to staff audit; assert decision, actor label, and exact server time"
  },
  {
    "id": "link-expiry",
    "milestone": "M1",
    "claim": "Client links last seven days, then cannot read or submit the request",
    "where": "landing fact, share-link panel, expired client state",
    "test": "npm run test:e2e -- --grep @claim:link-expiry",
    "sandbox": "fresh demo; assert a published link expires exactly seven days after server time; use an expired token; attempt client read and direct submission; assert both deny content"
  },
  {
    "id": "secure-upload",
    "milestone": "repair",
    "claim": "A client PDF is type-checked, malware-scanned, and scoped",
    "where": "landing, upload client action, README",
    "test": "npm run test:e2e -- --grep @claim:secure-upload",
    "sandbox": "fresh demo; publish only the upload action; assert an EICAR fixture is rejected, a PDF is accepted, and the clean result reaches the staff audit"
  },
  {
    "id": "choice-flow",
    "milestone": "repair",
    "claim": "A client can choose one listed option through a scoped link",
    "where": "landing, choice client action, README",
    "test": "npm run test:e2e -- --grep @claim:choice-flow",
    "sandbox": "fresh demo; publish the choice action; select the square crop; assert the exact option reaches the staff audit"
  },
  {
    "id": "external-link",
    "milestone": "repair",
    "claim": "A client sees the destination before opening an HTTPS payment or booking link",
    "where": "landing, external client action, README",
    "test": "npm run test:e2e -- --grep @claim:external-link",
    "sandbox": "fresh demo; publish the external action; assert the host is disclosed, the server returns only its configured HTTPS URL, and the audit records the open without claiming payment"
  },
  {
    "id": "reminder-audit",
    "milestone": "repair",
    "claim": "Staff can schedule one reminder and see its audit record",
    "where": "demo action controls, README",
    "test": "npm run test:e2e -- --grep @claim:reminder-audit",
    "sandbox": "fresh demo; schedule a reminder for the open approval; assert the server time and staff actor appear in the audit"
  },
  {
    "id": "real-workspace",
    "milestone": "repair",
    "claim": "A firm starts with an empty, isolated workspace that persists",
    "where": "workspace onboarding, README",
    "test": "npm run test:e2e -- --grep @claim:real-workspace",
    "sandbox": "local auth fixture; create a named firm workspace with no seed data; create and publish an approval; complete it as a signed-out client; assert persistence and deny a second owner"
  },
  {
    "id": "demo-privacy",
    "milestone": "repair",
    "claim": "Demo traffic stays on this site, data lasts at most 24 hours, and leaving deletes the room",
    "where": "landing privacy section, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:demo-privacy",
    "sandbox": "fresh demo; record every browser request; exchange a fragment link; assert the fragment is removed and never requested; leave; assert the old room is unavailable"
  },
  {
    "id": "staff-auth",
    "milestone": "repair",
    "claim": "Staff access rejects missing or invalid tokens",
    "where": "workspace sign-in page, README",
    "test": "npm run test:e2e -- --grep @claim:staff-auth",
    "sandbox": "fresh browser context; request the staff workspace without a token and with an invalid token; assert 401 and WWW-Authenticate for both"
  }
]
